"""
Setup configuration for MoAgent.

This is a minimal setup.py for backward compatibility.
Primary build configuration is in pyproject.toml.
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
